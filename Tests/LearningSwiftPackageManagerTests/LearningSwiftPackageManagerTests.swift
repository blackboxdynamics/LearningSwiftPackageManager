import XCTest
@testable import LearningSwiftPackageManager

final class LearningSwiftPackageManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LearningSwiftPackageManager().text, "Hello, World!")
    }
}
